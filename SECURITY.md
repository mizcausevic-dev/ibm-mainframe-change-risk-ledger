# Security

Do not commit credentials, hostnames, dataset names from live systems, mainframe connection strings, RACF identifiers, customer account data, or production JCL.

This repository uses synthetic fixtures and sample artifacts only. If adapting it to a real estate, aggregate change telemetry before export and remove job-level identifiers that could reveal production topology.
